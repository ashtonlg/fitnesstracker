services:
  api:
    build: ./backend
    environment:
      - DATABASE_URL=sqlite:////data/app.db
    volumes:
      - ./data:/data
    ports:
      - "8000:8000"

  web:
    build: ./frontend
    depends_on:
      - api
    ports:
      - "8080:80"
