[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["frontend", "data", "node_modules", "dist"]

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]

[tool.ruff.format]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
addopts = "-q"
pythonpath = ["backend"]
